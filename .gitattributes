# Auto-generated site payload — always keep local copy on merge conflicts.
# The bot regenerates this file on every cron tick, so discarding the remote
# version during a pull is safe; the next automation run overwrites it anyway.
# docs/site/data.json merge=ours
